{
  "name": "engram-hotdox-keys",
  "version": "1.0.0",
  "description": "3d models for my hotdox76v2 mapped to Arno's Engram layout",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf ./stl && rm -rf ./scad && rm -rf ./rows",
    "generate": "node build/index.js",
    "start": "npm run clean && npm run generate",
    "prestart": "npm run build"
  },
  "author": "Travis Reed (@dancxjo)",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^18.14.6",
    "@types/yargs": "^17.0.23",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "mkdirp": "^2.1.5",
    "yargs": "^17.7.1"
  }
}
