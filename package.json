{
  "name": "engram-hotdox-keys",
  "version": "1.0.0",
  "description": "3d models for my hotdox76v2 mapped to Arno's Engram layout",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf ./stl && rm -rf ./scad",
    "generate:scad": "node build/index.js",
    "generate:stl": "mkdir -p stl && for f in ./scad/*.scad; do echo $f; openscad-nightly -o stl/$(basename $f .scad).stl $f; done",
    "start": "",
    "prestart": "npm run build"
  },
  "author": "Travis Reed (@dancxjo)",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^18.14.6",
    "typescript": "^4.9.5"
  }
}
